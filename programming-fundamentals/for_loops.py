# For Loops

instance_ids = ["i-123456abcs", "i-234567", "i-abcdefg123"]
for instance_id in instance_ids:
    print(f"Checking status of instance {instance_id}")
    # code to execute instance status
    print(f"instance {instance_id} statys check complete.")

print("All instances have been checked.")
